import React from 'react'

type Props = {
    language?: string | undefined;
    code: string;
}

const CodeBlock = (props: Props) => {
  return (
    <div>CodeBlock</div>
  )
}
export default CodeBlock