import React from 'react'

type Props = {
    headers: string[];
    rows: {
    cells: string[];
}[]
}

const TableBlock = (props: Props) => {
  return (
    <div>TableBlock</div>
  )
}
export default TableBlock